import { LiveCode } from 'components'
import {LoaderCircle} from './circle/ReactCircle.js' 
import {LoaderCog} from './cog/cog.js' 
import {LoaderRedo} from './redo/redo.js' 
import {LoaderSpinner} from './spinner/spinner.js' 
const html = require('!!raw-loader!./circle/Circle.html').default
const react = require('!!raw-loader!./circle/ReactCircle.js').default
const vue = require('!!raw-loader!./circle/VueCircle.js').default
const ReactCircleCode= {react,html,vue} 
const ReactCircleComponent = `<LoaderCircle />`
const func2 = 
`<LoaderCog 
  color="primary-500"
  custom="blue"
  size={30} 
/>`
const func3 = `
<LoaderRedo
  color="primary-dark"
  size={30} 
 />`
const func4 = `
<LoaderSpinner
  color="primary-light"
  size={50} 
 />
 `

<h1>Loaders</h1>

<LiveCode
    code={ReactCircleComponent}
    copy={ReactCircleCode["react"]}
    scope={{ LoaderCircle }}
    title="Circle Loader"
/>

<LiveCode
    code={func2}
    copy={require('!!raw-loader!./cog/cog.js').default}
    scope={{ LoaderCog }}
    title="Cog Loader"
    description="The custom prop (overrides color prop)"
/>

<LiveCode
    code={func3}
    copy={require('!!raw-loader!./redo/redo.js').default}
    scope={{ LoaderRedo }}
    title="Redo Loader"
    description="color: primary-dark"
/>

<LiveCode
    code={func4}
    copy={require('!!raw-loader!./spinner/spinner.js').default}
    scope={{ LoaderSpinner }}
    title="Spinner Loader"
    description="size: 50, default size is 30"
/>